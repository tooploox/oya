Project: project

build: |
       printf "build run"
       if [ ! $# -eq 0 ]; then
           printf "\nArgs: $@";
       fi
       if [[ ! -z "${Oya[Flags.flag1]}" ]]; then
           printf "\nFlags: ${Oya[Flags.flag1]} ${Oya[Flags.flag2]}";
       fi
