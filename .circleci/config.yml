version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.11.4

    steps:
      - checkout
      - run: go version
      - run: go run oya.go run develop
      - run: go run oya.go run importTestKeys
      - run: go run oya.go run test
      - run: go run oya.go run removeTestKeys

  release:
    docker:
      - image: circleci/golang:1.11.4
    steps:
      - checkout
      - add_ssh_keys
      - run: go run oya.go run release

workflows:
  version: 2
  build-release:
    jobs:
      - build
      - release:
          requires:
            - build
          filters:
            branches:
              only: master
